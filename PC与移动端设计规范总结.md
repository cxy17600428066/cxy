# PC 与移动端设计规范总结

## 1. 文档说明

- 适用范围：
  - PC 端工作台原型：[全渠道商品内容平台-PC工作台.html](C:\Users\admin\Documents\New project\全渠道商品内容平台-PC工作台.html)
  - 移动端预览原型：[商品手册移动端预览-弹窗详情.html](C:\Users\admin\Documents\New project\商品手册移动端预览-弹窗详情.html)
- 目标：统一本次产品手册生成流程在 PC 端与移动端的视觉语言、布局逻辑、组件样式与交互规则。
- 设计基调：贴近现有业务平台风格，采用蓝白后台工作台视觉，强调清晰、规整、可编辑、信息密度适中。

## 2. 整体设计原则

### 2.1 风格定位

- 以“平台工作台”而非“营销落地页”为核心方向。
- 主视觉采用蓝白体系，减少暖色大面积铺陈，仅在提示、强调、风险状态中做少量辅助色点缀。
- 页面保持轻玻璃质感与卡片分层，兼顾业务工具属性和品牌展示感。

### 2.2 布局原则

- 内容优先，减少无效留白，避免模块过高、过宽或纵向拖沓。
- 同一层级模块采用统一卡片化容器，圆角、边框、阴影保持一致。
- 模块之间通过固定间距分层，禁止出现重叠、遮挡、文本溢出、按钮错位。
- PC 端优先保证高信息密度和快速操作；移动端优先保证单手浏览和阅读节奏。

### 2.3 交互原则

- 默认先展示摘要，深度编辑通过弹窗或展开操作完成，节省主界面空间。
- 关键操作必须有明确状态反馈，如选中、展开、收起、禁用、删除、替换、上移、下移。
- 重要动作按钮对齐，文案统一，避免同页中按钮风格混乱。
- 跨端字段语义保持一致，商品卖点、场景、特效、标签等内容需同步映射。

## 3. 视觉基础规范

### 3.1 色彩规范

#### 主色与中性色

- 品牌主蓝：`#2258f6`
- 深蓝主色：`#12317a`
- 亮蓝辅助：`#3c8dff`
- 页面背景：`#edf3f8`
- 浅层背景：`#f7fbff` / `#f8fbff`
- 卡片背景：`rgba(255,255,255,.92)` 到 `#ffffff`
- 分割线/描边：`#d9e4ef`、`#c2d3e5`
- 主文本：`#132033`
- 次级文本：`#607086`

#### 功能色

- 成功状态：`#149c6e`
- 强调辅助色：`#ff9a3d`
- 风险/移除按钮：浅红底 + 柔和红字，不使用高饱和深红背景

### 3.2 字体规范

- 中文字体优先：
  - PC：`"Segoe UI Variable", "PingFang SC", "Microsoft YaHei", sans-serif`
  - Mobile：`"Source Han Sans SC", "Microsoft YaHei UI", "PingFang SC", sans-serif`
- 标题采用更高字重，突出结构层级。
- 建议字号体系：
  - 页面主标题：30px 左右
  - 一级模块标题：18px 至 22px
  - 卡片标题：15px 至 16px
  - 正文：13px 至 14px
  - 辅助说明：11px 至 12px

### 3.3 圆角与阴影

- 外层面板圆角：22px 至 28px
- 中型卡片圆角：18px 至 24px
- 标签/按钮圆角：14px 或 999px
- 阴影保持轻量化，主用于层级区分，不做厚重投影。

### 3.4 间距与行高

- 页面主间距控制在 12px、14px、16px、20px、24px 体系内。
- 行高需兼顾信息压缩与可读性：
  - 正文建议 `1.6` 到 `1.8`
  - 标题建议 `1.15` 到 `1.35`
- 避免过高行距导致页面松散，也避免过低行高影响阅读。

## 4. 组件通用规范

### 4.1 按钮规范

- 主按钮、次按钮、幽灵按钮、危险按钮统一使用圆角矩形，不使用风格跳脱的异形按钮。
- 按钮高度优先保持统一：
  - PC：38px 至 42px
  - Mobile：42px 至 46px
- 同一区域按钮必须对齐，左右间距一致。
- 主按钮使用蓝色渐变或浅蓝强化；次按钮使用白底浅蓝边框；危险按钮使用浅红背景与红色文字。

### 4.2 标签规范

- 标签统一使用胶囊形态。
- 普通标签采用浅蓝背景、蓝色文字。
- 场景标签、状态标签可使用蓝色强化版，但不得过深影响正文阅读。
- 页面目录、筛选项、商品标签、效果标签均采用统一标签语言。

### 4.3 卡片规范

- 商品卡、编辑卡、目录卡、信息卡统一使用白底或浅蓝白渐变底。
- 卡片内容上下边距应紧凑，避免大面积空白。
- 图片、标题、标签、摘要、按钮遵循固定上下顺序，保证浏览效率。

### 4.4 弹窗规范

- 弹窗统一为居中浮层，带遮罩与圆角大卡片。
- 标题区、内容区、操作区分层明确。
- 弹窗按钮需继承页面统一按钮体系。
- 图片区、长图区、企微占位区统一为蓝白底，不保留旧暖色样式。

## 5. PC 端规范

## 5.1 页面结构

- 顶部为工作台头部区域，包含标题、关键统计、步骤导航。
- 主体采用左主内容区 + 右侧“当前选择商品”侧栏结构。
- 右侧仅保留“当前选择商品”，已移除“流程摘要”“原型目标”。
- 页面内容按流程分为 5 步：
  - 第 1 步：选择商品
  - 第 2 步：选择用户场景
  - 第 3 步：编辑文档内容
  - 第 4 步：预览生成的产品手册
  - 第 5 步：导出产品手册或生成分享链接

### 5.2 第 1 步：选择商品

#### 筛选区规范

- 默认展示 2 个筛选：
  - 名称搜索
  - 渠道选择
- 默认状态下一行展示 2 个筛选卡片，收纳信息，提升紧凑度。
- 点击“展开全部筛选”后显示全部筛选条件；点击后可收起。
- 筛选项使用统一胶囊按钮样式，选中态高亮明确。
- 增加“全选当前结果”能力，并与筛选结果联动。

#### 商品卡规范

- 默认展示商品主图、商品名、基础信息、标签、简短描述。
- 价格信息不能使用过深背景，需与整体蓝白风格协调。
- 商品卡按钮风格与第 3 步“商品内容”区域保持统一。
- 按钮需横向对齐，避免高低不一。

#### 当前选择商品侧栏

- 右侧只展示当前已选商品，不展示无关摘要模块。
- 已选商品支持移除，但必须至少保留 1 个，不能全部移空。
- 已选商品列表需要展示名称、基础信息与标签，支持滚动查看。

### 5.3 第 3 步：编辑文档内容

#### 模块划分

- 内容拆分为两块：
  - 固定内容
  - 商品内容
- 固定内容默认展开，支持手动收起。
- 商品内容按“单商品一张卡片”展示。

#### 商品内容展示规则

- 默认只显示商品名称、图片、基础信息、摘要预览、标签/场景/卖点概览。
- 单个商品的“编辑”按钮放在该商品卡片下方工具区。
- 点击“编辑”后以弹窗形式维护详细内容，不在主界面直接展开长表单。
- 编辑字段包括但不限于：
  - 一句话优势
  - 卖点
  - 场景
  - 特效
  - 标签

#### 编辑交互规范

- 编辑弹窗应展示当前商品名与基础信息。
- 所有字段按信息属性分组，避免长表单无层次堆叠。
- 编辑完成后的内容需同步回商品卡预览摘要，并同步到移动端详情展示。

### 5.4 第 4 步：预览生成的产品手册

#### 页面目录规范

- 页面目录只展示：
  - 标题
  - 标签
- 不展示额外副标题或冗余描述。
- 每个小页签内容水平、垂直都需居中。
- 页签留白需适中，不能贴边，也不能显得过空。

#### 页面编辑能力

- 每页支持直接替换图片。
- 每页支持删除，删除页在后续导出 PDF 时不展示。
- 每页支持上移、下移，导出顺序按当前顺序执行。
- 删除页可在已删除区域中恢复。

#### 分页规范

- 根据 PDF 单页大小进行内容分页。
- 商品内容过长时，自动拆分为多页展示。
- 分页仅调整承载方式，不修改原始内容表达。

### 5.5 PC 端分享与多端预览

- 支持 PC、平板、手机三种视口切换。
- 预览框统一放在平台化容器内，不使用与整体风格割裂的边框和配色。

## 6. 移动端规范

### 6.1 整体结构

- 采用单列纵向阅读流，页面宽度控制在 430px 内。
- 顶部为吸顶导航栏，保持品牌名、版本信息与浏览连续性。
- 内容分为：
  - 品牌封面
  - 品牌介绍
  - 产品手册
  - 渠道与系统实力
  - 招商联系

### 6.2 视觉风格

- 与 PC 端保持同一蓝白体系和品牌渐变头图。
- 卡片背景统一为白底或浅蓝白渐变，不保留旧暖色大面积背景。
- 图片展示区、长图展示区、企微占位区统一为蓝白底。

### 6.3 商品展示规则

- 默认展示当前商品名称、摘要、图片、核心信息。
- 商品详情不在主页面完全展开，通过弹窗查看完整内容。
- 商品切换使用横向标签或产品切换条，交互轻量直观。

### 6.4 详情弹窗规范

- 弹窗展示商品详情、卖点、场景标签、图集与补充信息。
- 弹窗关闭按钮、主操作按钮统一使用蓝白体系按钮。
- 弹窗头部、图片区、信息区、按钮区需层级清晰。

### 6.5 联系转化区

- 底部联系区与页面整体风格保持一致。
- 悬浮联系条与页面联系卡均使用统一按钮体系：
  - 主按钮：联系/咨询
  - 次按钮：复制企微/查看方式
- 企微区域当前可作为占位设计，正式上线后替换为真实二维码或官方活码。

## 7. 跨端一致性规范

### 7.1 信息一致性

- PC 端编辑的商品内容需同步到移动端详情展示。
- 同一商品在两端的名称、规格、口味、渠道、标签、卖点、场景、特效应保持一致。
- 允许两端在信息呈现密度上不同，但不能出现字段含义不一致。

### 7.2 样式一致性

- 主色、边框、按钮、标签、卡片层级统一。
- 移动端沿用 PC 端的品牌蓝白风格，不额外引入新的主题方向。
- 删除旧暖色残留样式，避免同一套原型中出现双主题并存。

### 7.3 交互一致性

- 默认先看摘要，再进行编辑或查看详情。
- 展开、收起、切换、选中、删除、恢复等动作在两端都应具备明确状态。
- 高风险操作必须保留禁用条件或显式限制，例如“至少保留一个已选商品”。

## 8. 本次改版关键落地结论

- 平台风格从偏暖色原型，统一调整为更贴近真实系统的蓝白工作台风格。
- PC 端第 1 步筛选区改为“默认简洁 + 按需展开”的结构。
- PC 端右侧信息聚焦到“当前选择商品”，减少干扰。
- 第 3 步商品内容改为摘要卡片 + 弹窗编辑，兼顾信息完整和页面紧凑。
- 第 4 步目录与页面操作能力增强，支持替换、删除、排序与分页。
- 移动端详情、长图、企微弹窗等区域已与上方页面风格统一。
- 两端均以“不重叠、不遮挡、不留过多空白、按钮对齐、字体合理”为基础标准。

## 9. 后续延展建议

- 后续如进入正式开发，可将当前规范沉淀为设计 token：
  - 颜色 token
  - 圆角 token
  - 阴影 token
  - 间距 token
  - 按钮与标签组件 token
- 建议补充一份字段映射表，明确 PC 编辑字段与移动端展示字段的一一对应关系。
- 建议继续整理“异常态规范”，包括空数据、超长文案、超多标签、图片缺失、替换失败、导出失败等场景。
